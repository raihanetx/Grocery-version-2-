# Only ignore very large generated files that can be regenerated
# Everything else is included for portability

# Large generated folders (regenerate with: bun install)
node_modules
.next

# OS files
.DS_Store
